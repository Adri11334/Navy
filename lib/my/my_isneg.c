/*
** EPITECH PROJECT, 2021
** task04 cpoolday03
** File description:
** functions that returns P if n is pos/null and N if neg
*/

int my_isneg(int n)
{
    if (n < 0)
        return (1);
    else if (n >= 0) {
        return (0);
    }
    return (0);
}
